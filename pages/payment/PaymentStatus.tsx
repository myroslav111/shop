import { FC } from 'react';

import PaymentStatusPage from '@/components/screens/payment/PaymentStatusPage';

const PaymentStatus: FC = () => {
    return <PaymentStatusPage />;
};

export default PaymentStatus;
